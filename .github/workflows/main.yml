name: CI
"on":
  push: null
  schedule:
  - cron: 01 13 * * SAT
jobs:
  build:
    runs-on: windows-2019
    steps:
    - name: Build
      run: |
        go install mvdan.cc/gofumpt@latest
        gofumpt -w $<
        goreleaser build --single-target --snapshot --clean
